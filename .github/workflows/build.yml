# Hinweis:
# Diese Workflow-Datei ist öffentlich, enthält aber keine geheimen Daten.
# Secrets wie GHCR_PAT sind in den GitHub Actions Secrets hinterlegt
# und werden nicht im Repository gespeichert.

name: Build & Push Docker

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      # Logge dich mit deinem PAT ein (nicht GITHUB_TOKEN)
      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GHCR_PAT }}

      - name: Build and push Docker image
        uses: docker/build-push-action@v4
        with:
          context: .
          push: true
          tags: ghcr.io/kroeberd/jdownloader-discord-monitor:latest
          platforms: linux/amd64,linux/arm64
